// add resolver logic
const { Profile } = require('../models');
// add in authenticators to the code

const resolvers = {
    // add resolvers for the query and mutations

};

// maybe add in the ability to add or remove stuff from the profiles


module.exports = resolvers;
